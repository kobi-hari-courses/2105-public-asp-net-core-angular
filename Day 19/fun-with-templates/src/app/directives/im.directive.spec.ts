import { ImDirective } from './im.directive';

describe('ImDirective', () => {
  it('should create an instance', () => {
    const directive = new ImDirective();
    expect(directive).toBeTruthy();
  });
});
